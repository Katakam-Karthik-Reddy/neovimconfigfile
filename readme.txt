// If can either just cloned this repo or download just fullinitconfig file follow below
place it in $HOME/.config/nvim in linux and <user>/Appdata/local/nvim/ in windows
must install programs:
1. gcc
2. node
3. git
4. fd (choco install -g fd)
5. yarn
